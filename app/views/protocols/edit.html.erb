<h3>Editing protocol</h3>

<% tagged_form_for(@protocol) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'protocol_form', :locals => {:f => f}%>
  
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @protocol %> |
<%= link_to 'List All', protocols_path %>
