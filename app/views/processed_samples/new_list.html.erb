<h1>Select source sample for extraction</h1>

<%= form_tag url_for(:action => :new), :method => :get do %>

<table class="list">
<tr>
  <th></th>
  <%= render :partial => 'samples/sample_hdr', :locals => {:row_tags => 'no'} %>
</tr>

<% for sample in @samples %>
  <tr>
  <td><%= radio_button_tag(:source_id, sample.id)%></td>
  <%= render :partial => 'samples/sample_row', :locals => {:sample => sample,
                                                           :row_tags => 'no'} %>
  </tr>
<% end %>
</table>

<%= submit_tag 'Select'%>
<% end %>