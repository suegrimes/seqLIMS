<h3>New patient/sample</h3>

<% form_for(@patient) do |f| %>
  <%= f.error_messages %>
  
  <%= render :partial => 'patient_form',      :locals => {:f => f, :patient_id => @patient.id} %>
  
  <p><%= f.submit "Create" %></p>
  <% end %>

<%= link_to 'Back', :back %>
